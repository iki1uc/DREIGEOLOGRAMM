export function generate81() {
  const matrix = {};
  const letters = ["d","e","f","g","h","i","j","k","w"];

  letters.forEach(letter => {
    matrix[letter] = [];
    for (let n = 1; n <= 9; n++) {
      matrix[letter].push(`${letter}${n}`);
    }
  });

  return matrix;
}
